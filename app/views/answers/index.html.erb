<% content_for(:title, t('.title')) %>
<div class="pb-6 sm:pb-8 lg:pb-12 mt-20">
  <div class="max-w-screen-2xl px-4 md:px-8 mx-auto">
    <div class="flex justify-between text-gray-600 mb-16">
      <p>
        質問に対する回答を元にマッチしそうな情報をピックアップします。<br>
        それぞれの選択肢の中から、どちらに当てはまるか選んでください。<br>
      </p>
      <%= link_to design_tips_path(q: { tags_name_cont: 'テクニック' }), class: "btn font-serif bg-green-700 hover:bg-green-600 text-white text-center rounded-lg" do %>
        情報一覧はこちら
      <% end %>
    </div>

    <%= turbo_frame_tag 'search' do %>
      <%= form_with(url: answers_path, method: 'get', remote: true) do |f| %>
        <div class= 'font-serif xl:text-lg'>Web系のサービスやデザインの作成に関わった経験は？</div>
        <div class="mb-6 md:mb-8">
          <%= f.radio_button :answer1, "A", required: true %> 全くない
          <%= f.radio_button :answer1, "B" %> 少しはある
        </div>

        <div class= 'font-serif xl:text-lg'>あなたがデザインする際に重視したいのは？</div>
        <div class="mb-6 md:mb-8">
          <%= f.radio_button :answer2, "A", required: true %> 使いやすさ
          <%= f.radio_button :answer2, "B" %> おしゃれさ
        </div>

        <div class= 'font-serif xl:text-lg'>あなたの学習スタイルは？</div>
        <div class="md:mb-8">
          <%= f.radio_button :answer3, "A", required: true %> まずは基本を抑える
          <%= f.radio_button :answer3, "B" %> 手を動かしながら覚える
        </div>

        <%= f.submit "回答する", data: { turbo_frame: 'search' }, class: "btn bg-green-700 hover:bg-green-600 text-white font-serif rounded-lg" %>
      <% end %>

      <div class="container mx-auto pt-3 grid sm:grid-cols-2 xl:grid-cols-3 gap-8 md:gap-12 xl:gap-16 pt-8">
        <% @answer_design_tip.each do |answer_design_tip| %>
          <% if answer_design_tip.answer.answer_code == @answer_code %>
            <%= render 'design_tips/design_tip', design_tip: answer_design_tip.design_tip %>
          <% end %>
        <% end %>
      </div>
    <% end %>
  </div>
</div>
